create table mac_traffics (
       mac string,
       datapath_id text,
       packet integer,
       byte integer
       );

create table switch_traffics(
       datapath_id integer,
       packet integer,
       byte integer
       );
